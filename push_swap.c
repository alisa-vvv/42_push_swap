#include "push_swap.h"

int	main(int argc, char **argv)
{
	if (argc == 1)	
	{
		ft_printf("argc is one, remove this later\n");
		exit(0);
	}
	ft_printf("argc: %d\n", argc);
	ft_printf("first argument: %s\n", argv[1]);
	return (0);
}
